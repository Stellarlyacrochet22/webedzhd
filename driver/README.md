# HWID Spoofer Kernel Driver (placeholder)

Requires Windows Driver Kit (WDK).

This directory will contain:

- `hwid_filter.sys` — disk serial filter driver
- `hwid_filter.inf` — installation INF
- `sources` / `CMakeLists.txt` WDK integration

See `docs/build.md` for test-signing setup.
